-- create database aaaaaa;
-- use aaaaaa;

/*create table pessoas (
	cod int NOT null,
    nome varchar(20),
    idade int,
    PRIMARY KEY (cod)
)*/

/*INSERT INTO pessoas VALUES (0, 'Bruno', 20), (1, 'Rafael', 24), (2, 'Felipe', null),
(3, 'Carol', NULL), (4, "Iago", null)*/

-- DELETE FROM pessoas WHERE cod < 2;

SELECT * FROM pessoas;